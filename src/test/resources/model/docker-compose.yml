version: "3.9"
services:
  similarity:
    image: similarity:0.3.0
    ports: ["5000"]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 10s
      retries: 5
